spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://mysql_user:3306/user_service?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  # 🔥 BẬT LẠI REDIS CONFIG
  data:
    redis:
      host: redis
      port: 6379
      timeout: 5000ms

server:
  port: 8081